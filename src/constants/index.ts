export enum FETCH_STATUS {
    LOADING= 'loading',
    IDLE= 'idle',
}

const LG = 164;
const MD = 64;
const SM = 40;
const EXT_SM = 20;

export enum ICON_SIZE {
    LARGE_ICON = LG,
    MEDIUM_ICON = MD,
    SMALL_ICON = SM,
    EXTRA_SMALL_ICON = EXT_SM,  
}
    